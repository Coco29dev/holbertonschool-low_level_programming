#include "lists.h"


size_t print_list(const list_t *h)
{
int count = 0;
const list_t *current = h;
while (current != NULL)
{
current =  current->next;
count++;
if (str == NULL)
{
printf("[0] (nil)");
if (str != NULL)
{
printf("[len]", str);
}
return (count);
}
